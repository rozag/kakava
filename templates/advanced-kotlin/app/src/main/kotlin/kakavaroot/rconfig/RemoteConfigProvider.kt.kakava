package ${packageName}.rconfig

/**
 * An abstraction for the remote config provider
 *
 * Implement it with something like FirebaseConfigProvider. Basically,
 * it's just a key-value storage for the remote app customization.
 *
 * Feel free to extend it for your own needs.
 */
interface RemoteConfigProvider {
}